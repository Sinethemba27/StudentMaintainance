@model IEnumerable<StudentMaintainance.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Residence.Latitude)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoomNo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Residence.Latitude)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoomNo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.studentNo }) |
            @Html.ActionLink("Details", "Details", new { id=item.studentNo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.studentNo })
        </td>
    </tr>
}

</table>
